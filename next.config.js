/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['dashboard.leanow.vn'], // Add the domain you're using
  },
}

module.exports = nextConfig
